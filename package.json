{
  "name": "try-neurima",
  "version": "1.0.0",
  "private": true,
  "description": "Neurima static site for GitHub Pages",
  "scripts": {
    "build": "cd site && (npm ci || npm install) && npm run build",
    "dev": "cd site && npm run dev"
  }
}

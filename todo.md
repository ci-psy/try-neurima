## Launch TODO
1. Replace `APP_STORE_URL_PLACEHOLDER` with the final App Store URL in `src/app/page.tsx` and `src/app/faq/page.tsx`.
2. Run `scripts/set-app-store-url.sh "<app-store-url>"`.
3. Commit and push to `main`.
4. Verify GitHub Pages deploy completes.
5. Confirm `tryneurima.com` resolves to the new Pages URL.
